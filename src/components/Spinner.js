import React from 'react';
import { ActivityIndicator } from "react-native";

const Spinner = () => {
  return (
    <ActivityIndicator
      size={60}
      color="white"
    />
  );
}

export default Spinner;